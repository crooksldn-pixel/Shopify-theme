# PERSONA 8 — POST-PURCHASE (RUN 3)

Run 2: *can track.* Run 3: **can track and self-serve.**

TRACK ORDER holds its footer slot; TRACKING sits in the menu; and the FAQ now answers the
questions a waiting customer actually types — dispatch time, delivery window, "tracking says
delivered but I have nothing", returns process. The terms page backs it with LOST PARCELS and
FAULTS sections. This persona's run-1 dead end is now a three-surface path.

Still hosted, still unbranded: the account login (Times New Roman, no order lookup). And if
they email the address on the shipping policy, it bounces (`gmail.com.com`).

## EYEBALL-PASS REVISION (same day)

The scripted verdict "can track and self-serve" was generous. Reading the pages as this
persona: the FAQ promises an account-free lookup on the tracking page; the tracking page is a
sign-in wall whose SIGN IN lands on the unbranded hosted login, with guests told to find their
dispatch email. The self-serve returns route (AfterShip, order number + email) exists — linked
from TERMS, not from the tracking gate. Revised verdict: **can find tracking; cannot yet do
what the FAQ says they can.** Fix specified as RUN3-FINDINGS A6.
