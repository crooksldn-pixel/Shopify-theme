# PERSONA 5 — THE AIMLESS BROWSER (RUN 2)

Mobile, no intent, killing time. Run 1 verdict: *would return, would follow.* Run 2 verdict:
**same — and their return visit now has a date on it.**

Their journey is the homepage, and the homepage kept everything they stayed for: the attract-mode
board (pause guards re-verified: 0 fps off-screen and tab-hidden; the in-viewport fps sample read
lower this run under audit CPU load — treat as variance, the code is unchanged), CASE 001, the
WITNESS STATEMENT, REGISTER AS INFORMANT. The popup still fires here — correctly, since this is
the one page where the game pitch belongs, and this is the one persona who plays it.

Two additions serve them directly:

- **Filing dates** on four register cards give the return visit a hook — `FILED 03.08` reads
  exactly like the evidence log they enjoy, and tells them whether anything happened since.
- **The carriage status bar** rotates house copy (`[count] EXHIBITS CURRENTLY LOGGED`) in the
  same terminal voice.

Their return-hooks are still 2.6–5.9 viewports down; the homepage still opens on the board, not
a product. Run 1 judged that trade acceptable (this persona is why), and the commercial data
still says the homepage is not where money is lost.

**Outcome: returns, follows, plays the game.** Unchanged, by design.
