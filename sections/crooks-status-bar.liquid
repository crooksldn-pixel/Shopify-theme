{{ 'crooks.css' | asset_url | stylesheet_tag }}
<script src="{{ 'crooks-terminal.js' | asset_url }}" defer></script>

<section
  class="crk-root crk-status-bar"
  data-crk-section="status-bar"
  data-crk-rotate="{{ section.settings.rotate }}"
  data-crk-interval="{{ section.settings.interval_ms }}"
>
  <div class="crk-status">
    {%- liquid
          comment
            products_count includes the sets, which are a buying mechanism and never
            appear in the grid, so the headline would promise four pieces that are
            not there. Counted instead, skipping anything carrying
            crooks.set_components.
          endcomment
          assign crk_col_count = collections[section.settings.collection]
          assign crk_count = 0
          for crk_cp in crk_col_count.products
            unless crk_cp.metafields.crooks.set_components
              assign crk_count = crk_count | plus: 1
            endunless
          endfor
        -%}
    {%- for block in section.blocks -%}
      {%- assign crk_msg = block.settings.message | replace: '[count]', crk_count -%}
      {%- if crk_msg != blank -%}
        <p class="crk-status__msg" {{ block.shopify_attributes }}>{{ crk_msg }}</p>
      {%- endif -%}
    {%- endfor -%}
  </div>
</section>

{% schema %}
{
  "name": "Crooks — Status Bar",
  "tag": "section",
  "settings": [
    {
      "type": "checkbox",
      "id": "rotate",
      "label": "Rotate between messages",
      "info": "Rotation is always disabled for visitors who prefer reduced motion.",
      "default": true
    },
    {
      "type": "range",
      "id": "interval_ms",
      "label": "Seconds per message",
      "min": 3,
      "max": 20,
      "step": 1,
      "unit": "s",
      "default": 8
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection for [count]",
      "info": "Use [count] in any message to insert the number of products in this collection."
    }
  ],
  "blocks": [
    {
      "type": "message",
      "name": "Message",
      "settings": [
        { "type": "text", "id": "message", "label": "Message", "default": "FREE UK SHIPPING — ORDER BY 18:00 FOR SAME-DAY DISPATCH" }
      ]
    }
  ],
  "presets": [
    {
      "name": "Crooks — Status Bar",
      "blocks": [
        { "type": "message", "settings": { "message": "FREE UK SHIPPING — ORDER BY 18:00 FOR SAME-DAY DISPATCH" } },
        { "type": "message", "settings": { "message": "PROPERTY STORE — UNIT 7, LONDON" } },
        { "type": "message", "settings": { "message": "[count] EXHIBITS CURRENTLY LOGGED" } }
      ]
    }
  ]
}
{% endschema %}
