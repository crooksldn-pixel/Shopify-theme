# Product image backups

Original white-background product PNGs, kept before the background-removal
pass (2026-07). Each was cut out to transparent and re-uploaded to the live
store so products sit on the bone theme background instead of white boxes.

To restore any product's original image, re-upload the matching file here.

Not part of the theme — this folder is ignored by `shopify theme push`.
